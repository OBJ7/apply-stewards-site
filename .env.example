# Netlify environment variables

# Required for protected ops actions in /ops.html.
# Generate a long random value and set the same value in Netlify.
APPLY_ADMIN_TOKEN=replace_with_a_long_random_admin_token
